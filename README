Django faststart is the collection of templates for easy start of ne django application.

To make this template into running app copy Makefile.def.default into Makefile.def and settings_local.py.default into settings_local.py