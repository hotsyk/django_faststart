Please, follow code conventions used in this project:

 1. Documentation:
 	a) Every python file should begin with epydoc-format header, e.g:
	"""
	@version: 1.0
	@copyright: 2007-2008
	@license: TBD
	@status: production
	@summary: Base class for
	Creates temporary sqlite DB
	"""
	b) Every class should also begin with epydoc-format header:
	"""
    @summary: Base class for
    """
    c) Every function should also begin with epydoc-format header:
    """
    @summary: db mockup teardown
    @raise AttributeError:
    """
